# Logbook config

auth:
  # Basic auth settings. If username or password is blank, auth is disabled.
  # Default: disabled
  #
  # username: user
  # password: password

server:
  address: 127.0.0.1
  port: 11610

database:
  path: ../db

pagination:
  per_page: 100
